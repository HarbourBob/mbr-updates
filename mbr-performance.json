{
	"name": "MBR Performance",
	"slug": "mbr-performance",
	"version": "1.23.0",
	"download_url": "https://littlewebshack.com/?mbr_dl=4cab5825cabd3ef4",
	"checksum": "5cd8771a65e49ceac2f426f72f70056461e197a1de47eb1ddbbfbc485ae24eb9",
	"checksum_type": "sha256",
	"author": "<a href=\"https://littlewebshack.com\">Robert Palmer</a>",
	"author_homepage": "https://littlewebshack.com/about/",
	"homepage": "https://littlewebshack.com/mbr-performance/",
	"requires": "5.9",
	"tested": "7.0",
	"requires_php": "7.4",
	"last_updated": "2026-08-24 00:00:00",
	"sections": {
		"description": "Granular, transparent WordPress performance optimisation across core, scripts, fonts, preloading, database, images, WooCommerce and multisite — plus per-page and per-template Used CSS, and paste-in Critical CSS.",
		"changelog": "<h4>1.23.0</h4><ul><li>New: Used CSS Mode B — per-template critical CSS that genuinely removes the unused stylesheets rather than deferring them, so the bytes stop being downloaded at all. One cache entry per template instead of one per page. Off by default.</li><li>New: multi-sample template learning. Each template is learned from several distinct URLs and keeps the union of what they used, so a rule only one page needs still survives for the whole template. Learning visits are served untouched.</li><li>New: selector safelist — the guard rail for anything JavaScript adds after load (consent banners, cart drawers, modals, scroll-toggled classes), which a static analysis cannot see.</li><li>Safety: a stylesheet is only removed if that exact file was analysed while learning. Sheets carrying @import, print and narrow-media sheets, external sheets and excluded sheets are always left alone.</li><li>Safety: the inlined CSS replaces the first stylesheet it stands in for, preserving the original cascade position; media=\"screen\" rules are re-wrapped so they cannot leak into print.</li><li>Change: Mode A, Async CSS and Combine CSS all stand down when Mode B is on — the modes are alternatives. Hand-pasted Critical CSS still takes precedence over both.</li><li>New: the Performance Doctor understands both modes, recommends one or the other but never both, and reports whether Mode B is working, still learning, or leaving sheets alone by design.</li><li>New: ?mbrpe_modeb=off serves a single request with its original stylesheets, for comparing against the optimised page.</li></ul><h4>1.22.1</h4><ul><li>New: the Performance Doctor now understands script modules — it counts them, reports where the import map and preload hints landed, and recommends preload hoisting when it would help.</li><li>New: import map ordering check. A module printed in the head while the map sits in the footer cannot resolve its bare specifier imports; the Doctor now flags this as high priority.</li><li>New: the Doctor tells apart hoisting off, hoisting still learning the URL, and hoisting working — and tells block themes plainly that core already handles it.</li><li>Note: the learned module map is keyed on plugin version, so this upgrade clears it by design. Hoisted hints resume from the second visit to each URL.</li></ul><h4>1.22.0</h4><ul><li>New: Script Modules and Interactivity API support (WP 6.5+), including module preload hoisting that fixes late preload hints on classic themes.</li><li>New: static dependency graph walking, fetchpriority control, preload cap and exclusions. Off by default.</li></ul><h4>1.21.3</h4><ul><li>Fix: PDF report preview now renders as a centred A4 sheet instead of stretching to the browser width.</li><li>Fix: Note badge styling in reports; proper tab names on recommendation links.</li></ul><h4>1.21.2</h4><ul><li>Fix: real-user field data now appears in the Doctor site-scan view (it was being stripped by the info-tier roll-up filter).</li><li>Change: field data reported once site-wide rather than per template, and provisional notes now show the actual readings.</li></ul><h4>1.21.1</h4><ul><li>Fix: RUM field data now reaches the Performance Doctor immediately via on-demand aggregation, rather than waiting for the nightly cron.</li><li>New: \"Run aggregation now\" button; clearer status reporting; provisional p75s shown from 10 samples.</li></ul><h4>1.21.0</h4><ul><li>New: self-hosted Real User Monitoring — collects real-user Core Web Vitals (LCP, CLS, INP) into local tables and shows field data per template and per URL. No cookies, no IPs, nothing leaves your server. Off by default.</li><li>Hardening: the plugin's own mbrpe/v1 REST namespace is always permitted through REST hardening so the RUM beacon works for logged-out visitors.</li></ul><h4>1.20.1</h4><ul><li>Update checks now served from GitHub for reliable delivery.</li><li>Consolidated self-hosted release; corrected update manifest.</li></ul>",
		"installation": "Upload the plugin to <code>/wp-content/plugins/</code>, activate, and configure under MBR &rarr; Performance."
	},
	"icons": {
		"1x": "https://littlewebshack.com/updates/assets/mbr-performance-128.png",
		"2x": "https://littlewebshack.com/updates/assets/mbr-performance-256.png"
	},
	"banners": {
		"low": "https://littlewebshack.com/updates/assets/mbr-performance-772x250.png",
		"high": "https://littlewebshack.com/updates/assets/mbr-performance-1544x500.png"
	}
}
